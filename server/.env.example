# Server environment
DATABASE_URL=postgres://postgres:postgres@localhost:5432/bradbury_balance
PORT=8080

# K-anonymity minimum submissions to show period-level stats
K_ANON_MIN=8

# CORS allowlist (comma-separated). Use * ONLY for testing.
ALLOWED_ORIGINS=http://localhost:5173
